This is an example compare script for DOMJudge. You are encouraged to rename the files (and their references) in the build script, the c++ file, and the c++ header file. \
Make sure before archiving (with `zip`) and uploading to DOMJudge, that you run `make clean` to remove stuff that doesn't belong here.
